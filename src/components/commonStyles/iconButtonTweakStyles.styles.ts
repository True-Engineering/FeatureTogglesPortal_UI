import { ComponentStyles } from '@true-engineering/true-react-common-ui-kit';

export const styles = {
  s: {
    height: 40,
    minWidth: 40,
    maxWidth: 40,
  },

  l: {
    height: 48,
    minWidth: 48,
    maxWidth: 48,
  },

  xl: {
    height: 56,
    minWidth: 56,
    maxWidth: 56,
  },
};

export type IconButtonTweakStyles = ComponentStyles<typeof styles>;
