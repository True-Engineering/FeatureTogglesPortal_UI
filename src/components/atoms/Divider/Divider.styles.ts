import type { ComponentStyles } from '@true-engineering/true-react-common-ui-kit';

export const styles = {
  root: {
    width: '100%',
    borderTop: ['solid', 1],
  },
};

export type DividerStyles = ComponentStyles<typeof styles>;
