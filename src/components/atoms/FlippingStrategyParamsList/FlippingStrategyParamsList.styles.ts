import type { ComponentStyles } from '@true-engineering/true-react-common-ui-kit';

export const styles = {
  root: {
    fontSize: 14,
  },

  title: {
    paddingBottom: 2,
  },

  lightText: {},

  param: {
    paddingBottom: 2,
  },
};

export type FlippingStrategyParamsListStyles = ComponentStyles<typeof styles>;
